# YoloSegmentation
Yolo segmentation network launch

For first time use launch createVirtual in cmd. This will create virtual environment "Libraries" and install ultralytics package. Next time when launching project use startVirtual to activate environment

Videos/Images are placed in "Inputs" folder
